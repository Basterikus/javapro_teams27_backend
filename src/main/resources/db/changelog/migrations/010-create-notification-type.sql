CREATE TABLE notification_type (
                                   id serial PRIMARY KEY,
                                   code VARCHAR (255),
                                   name VARCHAR (255)
);